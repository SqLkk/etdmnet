org.gradle.java.home=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
kotlin.code.style=official

# Set to `true` to include the Android transport module in the build.
# Requires Android Gradle Plugin and Android SDK.
etdmnet.includeAndroid=false

# Android-specific (only consumed when etdmnet.includeAndroid=true)
android.useAndroidX=true
